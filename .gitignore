node_modules/
dist/
*.tsbuildinfo
.DS_Store

# Python bytecode from the plugins' ComfyUI entry points
__pycache__/
*.pyc

# Built frontends are release artifacts, not source: CI builds and publishes them. A plain
# `git clone` into custom_nodes therefore has no frontend until `pnpm build` runs.
plugins/*/js/

